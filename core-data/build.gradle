plugins {
    id 'com.android.library'
    id 'org.jetbrains.kotlin.android'
    id 'kotlin-kapt'
}

android {
    buildTypes {
        debug {
            buildConfigField "String", "BASE_URL", "\"http://192.168.227.51:5000/\""
        }
        release {
            buildConfigField "String", "BASE_URL", "\"http://10.0.2.2:5000/api/\""
        }
    }
    namespace 'com.kaleksandra.coredata'
}

dependencies {
    implementation(core)
    implementation(composeRuntime)

    implementation(retrofit)
    implementation(retrofitConverter)
    implementation(okHttp)
    implementation(loggingInterceptor)

    implementation(hiltDep)
    kapt(hiltKapt)

    implementation(serialization)
    implementation(dataStore)

    implementation(room)
    kapt(roomKapt)
}